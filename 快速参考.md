# 🚀 快速参考卡片

## 📡 API 端点

### 认证 API（无需 Token）
```bash
# 登录
POST http://localhost:8080/api/v1/auth/authenticate
Content-Type: application/json
{"username":"admin","password":"admin"}

# 注册
POST http://localhost:8080/api/v1/auth/register
Content-Type: application/json
{"firstname":"John","lastname":"Doe","email":"john@example.com","password":"pass123"}

# 刷新 Token
POST http://localhost:8080/api/v1/auth/refresh-token
Authorization: Bearer [refresh_token]
```

### 业务 API（需要 Token）
```bash
# 获取事件列表
GET http://localhost:8080/events
Authorization: Bearer [access_token]

# 获取组织者列表
GET http://localhost:8080/organizers
Authorization: Bearer [access_token]

# 登出
POST http://localhost:8080/api/v1/auth/logout
Authorization: Bearer [access_token]
```

---

## 🔑 测试账户

| 用户名 | 密码 | 角色 | 状态 |
|--------|------|------|------|
| admin | admin | ADMIN, USER | ✅ 启用 |
| user | user | USER | ✅ 启用 |
| disableUser | disableUser | USER | ❌ 禁用 |

---

## ⚙️ 配置信息

### 端口配置
- **应用端口**: 8080
- **数据库端口**: 3307（MySQL）
- **phpMyAdmin**: 9000

### Token 配置
- **Access Token**: 24 小时
- **Refresh Token**: 7 天
- **算法**: HS256

### Firebase 配置
- **Project ID**: se331lab10
- **Bucket**: se331lab10.appspot.com
- **Public URL**: https://storage.googleapis.com/{bucket}/{file}

---

## 🛠️ 常用命令

### 应用管理
```bash
# 编译
./mvnw clean package -DskipTests

# 启动
java -jar target/demo-331-bacnkend-0.0.1-SNAPSHOT.jar

# 后台启动
java -jar target/*.jar > app.log 2>&1 &

# 停止
kill $(lsof -t -i:8080)

# 查看日志
tail -f spring-boot-new.log
```

### 数据库管理
```bash
# 启动数据库
docker-compose up -d

# 停止数据库
docker-compose down

# 查看数据库日志
docker-compose logs db

# 访问 phpMyAdmin
open http://localhost:9000
```

### 快速测试
```bash
# 登录并保存 token
export TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/authenticate \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}' \
  | jq -r '.access_token')

# 使用 token 访问
curl -H "Authorization: Bearer $TOKEN" http://localhost:8080/events

# 刷新 token
curl -X POST http://localhost:8080/api/v1/auth/refresh-token \
  -H "Authorization: Bearer $TOKEN"
```

---

## 📝 快速问题排查

### 问题: 403 Forbidden
**原因**: Token 无效或未提供  
**解决**: 
```bash
# 1. 重新登录获取新 token
curl -X POST http://localhost:8080/api/v1/auth/authenticate \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}'

# 2. 确保 Header 正确
Authorization: Bearer [access_token]
```

### 问题: 401 Unauthorized
**原因**: 用户名或密码错误  
**解决**: 检查凭证是否正确

### 问题: Firebase 初始化失败
**原因**: 凭证文件不存在  
**解决**:
```bash
# 检查文件是否存在
ls src/main/resources/*-firebase-*.json

# 检查配置
grep firebase src/main/resources/application.yml
```

### 问题: 端口被占用
**原因**: 8080 端口已被使用  
**解决**:
```bash
# 查看占用进程
lsof -i :8080

# 停止进程
kill $(lsof -t -i:8080)
```

---

## 🔍 日志查看

### 查看特定日志
```bash
# 认证相关日志
tail -f spring-boot-new.log | grep -i "auth"

# 错误日志
tail -f spring-boot-new.log | grep -i "error"

# Firebase 相关日志
tail -f spring-boot-new.log | grep -i "firebase"

# JWT Token 日志
tail -f spring-boot-new.log | grep -i "token"
```

---

## 📊 健康检查

### 应用状态
```bash
# 检查应用是否运行
curl http://localhost:8080/actuator/health

# 检查端口监听
lsof -i :8080

# 检查进程
ps aux | grep java
```

### 数据库连接
```bash
# 测试 MySQL 连接
mysql -h localhost -P 3307 -u root -pcmu123wyh -e "SELECT 1"

# 检查数据库容器
docker ps | grep mysql
```

---

## 🎯 性能优化

### JVM 参数
```bash
java -Xms512m -Xmx1024m \
     -XX:+UseG1GC \
     -jar target/*.jar
```

### 数据库连接池
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
```

---

## 📚 文档索引

| 文档 | 说明 |
|------|------|
| `认证优化说明.md` | JWT 认证7项优化详解 |
| `Firebase存储优化说明.md` | 存储10项安全优化 |
| `JWT配置说明.md` | JWT 完整配置指南 |
| `优化完成总结.md` | 整体优化汇总 |
| `快速参考.md` | 本文档 |

---

## 🔐 安全检查清单

- [ ] 生产环境使用 HTTPS
- [ ] JWT Secret Key 使用环境变量
- [ ] 数据库密码加密存储
- [ ] Firebase 凭证文件权限设置
- [ ] 启用速率限制
- [ ] 配置 CORS 白名单
- [ ] 设置文件上传大小限制
- [ ] 定期清理过期 Token
- [ ] 监控异常登录行为
- [ ] 启用审计日志

---

## 🎉 快速开始

### 1分钟启动
```bash
# 1. 启动数据库
docker-compose up -d

# 2. 启动应用
java -jar target/demo-331-bacnkend-0.0.1-SNAPSHOT.jar

# 3. 测试登录
curl -X POST http://localhost:8080/api/v1/auth/authenticate \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}'

# 4. 访问 API
# 使用返回的 token 访问受保护接口
```

---

**💡 提示**: 将本文档收藏，随时查阅！

