# 🚀 后端项目服务访问指南

## ✅ 服务启动状态

### 当前运行的服务

| 服务 | 状态 | 端口 | 访问地址 |
|------|------|------|----------|
| **Spring Boot 后端** | ✅ 运行中 | 8080 | http://localhost:8080 |
| **MySQL 数据库** | ✅ 运行中 | 3307 | localhost:3307 |
| **phpMyAdmin** | 需要启动 | 8081 | http://localhost:8081 |

---

## 📊 phpMyAdmin 访问设置

### 方法1: 使用 Docker Compose 启动 phpMyAdmin

#### 1. 检查 `docker-compose.yml` 文件

如果项目中有 `docker-compose.yml`，检查是否包含 phpMyAdmin 配置：

```yaml
services:
  db:
    image: mysql:8.0
    container_name: se331_lab7_backend_0912-db-1
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: cmu123wyh
      MYSQL_DATABASE: selabdb

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: cmu123wyh
    depends_on:
      - db
```

#### 2. 启动 phpMyAdmin

```bash
# 如果 docker-compose.yml 中已有 phpmyadmin 配置
docker-compose up -d phpmyadmin

# 或者，单独运行 phpMyAdmin 容器
docker run --name phpmyadmin -d \
  --link se331_lab7_backend_0912-db-1:db \
  -p 8081:80 \
  -e PMA_HOST=db \
  -e PMA_PORT=3306 \
  phpmyadmin/phpmyadmin
```

#### 3. 访问 phpMyAdmin

- **URL**: http://localhost:8081
- **用户名**: `root`
- **密码**: `cmu123wyh`

---

### 方法2: 直接使用 MySQL 客户端

如果不想使用 phpMyAdmin，可以直接连接 MySQL：

#### 使用 MySQL Workbench

```
主机: localhost
端口: 3307
用户名: root
密码: cmu123wyh
数据库: selabdb
```

#### 使用命令行

```bash
# 通过 Docker exec 连接
docker exec -it se331_lab7_backend_0912-db-1 mysql -u root -pcmu123wyh selabdb

# 或使用本地 MySQL 客户端
mysql -h localhost -P 3307 -u root -pcmu123wyh selabdb
```

---

## 🔍 验证数据库数据

### 1. 查看所有表

```bash
docker exec se331_lab7_backend_0912-db-1 mysql -u root -pcmu123wyh selabdb -e "SHOW TABLES;"
```

**输出**:
```
Tables_in_selabdb
_user
_user_seq
action
event
event_images
organizer
token
token_seq
user_roles
```

### 2. 查看用户表（验证密码加密）

```bash
docker exec se331_lab7_backend_0912-db-1 mysql -u root -pcmu123wyh selabdb -e "
SELECT 
    id, 
    username, 
    email, 
    LEFT(password, 15) as password_prefix, 
    LENGTH(password) as pwd_length,
    enabled 
FROM _user;
"
```

**期望输出**:
```
id  username     email                  password_prefix      pwd_length  enabled
1   admin        admin@admin.com        $2a$10$2HaHSXw     60          1
2   user         enabled@user.com       $2a$10$mejFLbJ     60          1
3   disableUser  disableUser@user.com   $2a$10$hfAUYIM     60          0
```

✅ **验证通过**: 所有密码都是 BCrypt 加密（`$2a$10$` 前缀，60字符长度）

### 3. 查看用户角色

```bash
docker exec se331_lab7_backend_0912-db-1 mysql -u root -pcmu123wyh selabdb -e "
SELECT u.id, u.username, r.roles 
FROM _user u 
LEFT JOIN user_roles r ON u.id = r.user_id 
ORDER BY u.id, r.roles;
"
```

---

## 🧪 API 测试

### 1. 健康检查

```bash
curl http://localhost:8080/error
```

### 2. 用户登录

```bash
curl -X POST http://localhost:8080/api/v1/auth/authenticate \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}'
```

**期望响应**:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiJ9...",
  "refresh_token": "eyJhbGciOiJIUzI1NiJ9..."
}
```

### 3. 获取事件列表（需要 Token）

```bash
# 先获取 Token
ACCESS_TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/authenticate \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}' | jq -r '.access_token')

# 使用 Token 访问
curl http://localhost:8080/events \
  -H "Authorization: Bearer $ACCESS_TOKEN"
```

### 4. 测试 CORS（跨域）

```bash
curl -X OPTIONS http://localhost:8080/events \
  -H "Origin: http://localhost:3000" \
  -H "Access-Control-Request-Method: GET" \
  -v
```

---

## 📋 数据库表结构说明

### 核心表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| **_user** | 用户表 | id, username, email, password (BCrypt), enabled |
| **user_roles** | 用户角色关联表 | user_id, roles (ROLE_USER, ROLE_ADMIN) |
| **token** | JWT Token 记录 | user_id, token, token_type, expired, revoked |
| **event** | 事件表 | id, title, description, date, organizer_id |
| **organizer** | 组织者表 | id, name, image |
| **action** | 操作日志表 | id, action, timestamp |

---

## 🔒 密码验证快速脚本

我已为你创建了一个自动化验证脚本：

```bash
# 运行密码存储验证
chmod +x verify-password-storage.sh
./verify-password-storage.sh
```

**脚本功能**:
1. ✅ 检查 MySQL 容器状态
2. ✅ 验证密码格式（BCrypt）
3. ✅ 检查密码长度（60字符）
4. ✅ 测试登录功能
5. ✅ 测试错误密码拒绝
6. ✅ 生成安全性评估报告

---

## 📝 在 phpMyAdmin 中的操作

### 启动 phpMyAdmin 后

#### 1. 登录
- 打开 http://localhost:8081
- 用户名: `root`
- 密码: `cmu123wyh`

#### 2. 选择数据库
- 左侧点击 `selabdb`

#### 3. 查看用户表
- 点击 `_user` 表
- 点击 "Browse" 查看所有用户
- 点击 "Structure" 查看表结构

#### 4. 执行 SQL 查询

**查看密码加密格式**:
```sql
SELECT 
    id, 
    username, 
    email,
    SUBSTRING(password, 1, 15) as password_prefix,
    LENGTH(password) as password_length,
    enabled
FROM _user;
```

**查看用户角色**:
```sql
SELECT 
    u.id,
    u.username,
    u.email,
    GROUP_CONCAT(r.roles) as roles
FROM _user u
LEFT JOIN user_roles r ON u.id = r.user_id
GROUP BY u.id, u.username, u.email;
```

**查看 Token 记录**:
```sql
SELECT 
    t.id,
    u.username,
    t.token_type,
    t.expired,
    t.revoked,
    SUBSTRING(t.token, 1, 30) as token_prefix
FROM token t
JOIN _user u ON t.user_id = u.id
ORDER BY t.id DESC
LIMIT 10;
```

---

## 🚨 常见问题

### Q1: phpMyAdmin 无法连接数据库

**解决方法**:
```bash
# 检查 MySQL 容器是否运行
docker ps | grep mysql

# 重启 MySQL 容器
docker restart se331_lab7_backend_0912-db-1

# 检查网络连接
docker network ls
docker inspect se331_lab7_backend_0912-db-1 | grep NetworkMode
```

### Q2: 后端应用无法连接数据库

**检查配置**:
```yaml
# src/main/resources/application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/selabdb
    username: root
    password: cmu123wyh
```

### Q3: 密码显示为乱码

这是正常现象！BCrypt 密码包含二进制数据，在终端可能显示为乱码。
在 phpMyAdmin 中应该能正常显示。

### Q4: 无法登录

**检查步骤**:
1. 确认用户名和密码正确
2. 检查用户的 `enabled` 字段是否为 1
3. 查看应用日志: `tail -f spring-boot-new.log`
4. 验证密码是否为 BCrypt 格式

---

## 📊 当前系统状态

### 启动日志摘要

```
✅ Firebase Storage initialized successfully!
✅ CorsFilter registered in filter chain
✅ JwtAuthenticationFilter registered
✅ Application started in 3.081 seconds
```

### 服务端口映射

```
Spring Boot: 8080 (主应用)
MySQL:       3307 → 3306 (数据库)
phpMyAdmin:  8081 → 80 (管理界面，需手动启动)
```

---

## 🎯 快速启动 phpMyAdmin

### 一键启动命令

```bash
# 方法1: 使用 Docker Compose（如果配置了）
docker-compose up -d phpmyadmin

# 方法2: 单独运行容器
docker run --name phpmyadmin -d \
  --network container:se331_lab7_backend_0912-db-1 \
  -p 8081:80 \
  -e PMA_HOST=127.0.0.1 \
  -e PMA_PORT=3306 \
  -e PMA_USER=root \
  -e PMA_PASSWORD=cmu123wyh \
  phpmyadmin/phpmyadmin

# 方法3: 使用主机网络
docker run --name phpmyadmin -d \
  -p 8081:80 \
  -e PMA_HOST=host.docker.internal \
  -e PMA_PORT=3307 \
  -e PMA_USER=root \
  -e PMA_PASSWORD=cmu123wyh \
  phpmyadmin/phpmyadmin
```

### 启动后验证

```bash
# 检查容器状态
docker ps | grep phpmyadmin

# 查看日志
docker logs phpmyadmin

# 测试访问
curl http://localhost:8081
```

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| [密码存储验证报告.md](./密码存储验证报告.md) | BCrypt 密码加密详细验证 |
| [Security配置优化说明.md](./Security配置优化说明.md) | Spring Security 配置 |
| [项目优化总结.md](./项目优化总结.md) | 全项目优化汇总 |
| [verify-password-storage.sh](./verify-password-storage.sh) | 自动化验证脚本 |

---

## ✅ 验证清单

在 phpMyAdmin 中验证以下内容：

- [ ] 登录 phpMyAdmin (http://localhost:8081)
- [ ] 选择 `selabdb` 数据库
- [ ] 查看 `_user` 表，确认密码为 BCrypt 格式
- [ ] 验证密码前缀为 `$2a$10$`，长度为 60
- [ ] 查看 `user_roles` 表，确认角色分配
- [ ] 查看 `token` 表，确认 Token 记录
- [ ] 执行 SQL 查询，测试数据完整性

---

**🎉 后端项目已成功启动，可以开始验证了！**

**下一步**:
1. 启动 phpMyAdmin: `docker run --name phpmyadmin -d -p 8081:80 -e PMA_HOST=host.docker.internal -e PMA_PORT=3307 -e PMA_USER=root -e PMA_PASSWORD=cmu123wyh phpmyadmin/phpmyadmin`
2. 访问: http://localhost:8081
3. 登录并验证数据库数据

