# JWT 认证模块优化说明

## ✅ 已实现的7大优化

### 1️⃣ username vs email 一致性 ✅

**问题**：注册时只设置 email，未设置 username，导致后续查询失败

**解决方案**：
- `AuthenticationService.register()`: 统一设置 `username = email`
- 确保 JWT token 中的 subject 和数据库查询一致

```java
User user = User.builder()
        .username(request.getEmail())  // ✅ 设置 username = email
        .email(request.getEmail())
        // ...
        .build();
```

**影响的文件**：
- ✅ `AuthenticationService.java`
- ✅ `InitApp.java`

---

### 2️⃣ 刷新令牌旋转 (Refresh Token Rotation) ✅

**问题**：原代码返回原 refresh token，安全性不足

**解决方案**：
- 每次刷新都生成**新的 access token 和 refresh token**
- 旧的 refresh token 被标记为已撤销
- 降低 token 泄露风险

```java
// 🔧 修复2: 刷新令牌旋转
String newAccessToken = jwtService.generateToken(user);
String newRefreshToken = jwtService.generateRefreshToken(user);  // ✅ 新的 refresh token

revokeAllUserTokens(user);  // 撤销所有旧 token
saveUserToken(user, newAccessToken, TokenType.ACCESS);
saveUserToken(user, newRefreshToken, TokenType.REFRESH);

return AuthenticationResponse.builder()
        .accessToken(newAccessToken)
        .refreshToken(newRefreshToken)  // ✅ 返回新的 refresh token
        .build();
```

**影响的文件**：
- ✅ `AuthenticationService.refreshToken()`

---

### 3️⃣ 响应与错误码 ✅

**问题**：错误情况下返回空响应或 200 状态码

**解决方案**：
- 返回明确的 HTTP 状态码（400, 401）
- 返回结构化的 JSON 错误响应
- 统一错误响应格式

```java
private void sendErrorResponse(HttpServletResponse response, HttpStatus status, String message) {
    response.setContentType(MediaType.APPLICATION_JSON_VALUE);
    response.setStatus(status.value());
    
    Map<String, Object> errorBody = new HashMap<>();
    errorBody.put("error", status.getReasonPhrase());
    errorBody.put("message", message);
    errorBody.put("status", status.value());
    errorBody.put("timestamp", System.currentTimeMillis());
    
    new ObjectMapper().writeValue(response.getOutputStream(), errorBody);
}
```

**错误响应示例**：
```json
{
  "error": "Unauthorized",
  "message": "Invalid refresh token",
  "status": 401,
  "timestamp": 1697123456789
}
```

**影响的文件**：
- ✅ `AuthenticationService.refreshToken()`

---

### 4️⃣ 事务一致性 ✅

**问题**：撤销和保存 token 操作可能不一致

**解决方案**：
- 在 Service 方法上添加 `@Transactional`
- 确保"撤销旧 token + 保存新 token"要么都成功，要么都回滚
- 数据库操作的 ACID 特性

```java
@Transactional
public AuthenticationResponse authenticate(AuthenticationRequest request) {
    // ...
    revokeAllUserTokens(user);  // 撤销旧 token
    saveUserToken(user, jwtToken, TokenType.ACCESS);    // 保存新 access token
    saveUserToken(user, refreshToken, TokenType.REFRESH); // 保存新 refresh token
    // 要么都成功，要么都回滚
}
```

**影响的文件**：
- ✅ `AuthenticationService.register()`
- ✅ `AuthenticationService.authenticate()`
- ✅ `AuthenticationService.refreshToken()`

---

### 5️⃣ 访问控制与速率限制 ✅

**解决方案**：
- 明确配置认证端点的访问控制
- 添加注释提醒生产环境需要速率限制

```java
.authorizeHttpRequests((authorize) -> {
    authorize.requestMatchers("/api/v1/auth/register").permitAll();
    authorize.requestMatchers("/api/v1/auth/authenticate").permitAll();
    authorize.requestMatchers("/api/v1/auth/refresh-token").permitAll();
    // 注意：生产环境建议对 /refresh-token 添加速率限制
    // 可使用 Spring Cloud Gateway、Bucket4j 或 Redis 实现
    
    authorize.anyRequest().authenticated();
})
```

**生产环境建议**：
- 使用 **Bucket4j** 实现令牌桶算法
- 使用 **Redis** 存储速率限制计数器
- 限制：例如每个 IP 每分钟最多 10 次刷新请求

**影响的文件**：
- ✅ `SecurityConfiguration.java`

---

### 6️⃣ 内容类型 (Content-Type) ✅

**问题**：响应未设置 Content-Type，客户端可能解析失败

**解决方案**：
- 所有 JSON 响应都设置 `Content-Type: application/json`

```java
// 🔧 修复6: 设置正确的 Content-Type
response.setContentType(MediaType.APPLICATION_JSON_VALUE);
response.setStatus(HttpStatus.OK.value());

AuthenticationResponse authResponse = AuthenticationResponse.builder()
        .accessToken(newAccessToken)
        .refreshToken(newRefreshToken)
        .build();

new ObjectMapper().writeValue(response.getOutputStream(), authResponse);
```

**影响的文件**：
- ✅ `AuthenticationService.refreshToken()`
- ✅ `AuthenticationService.sendErrorResponse()`

---

### 7️⃣ 黑名单/撤销策略 ✅

**问题**：只持久化 access token，refresh token 未被持久化

**解决方案**：
- 引入 `TokenType` 枚举区分 ACCESS 和 REFRESH token
- 同时持久化两种类型的 token
- 验证时检查 token 类型
- 撤销时一并撤销所有类型的 token

```java
// TokenType 枚举
public enum TokenType {
  BEARER,      // 保留兼容性
  ACCESS,      // Access Token
  REFRESH      // Refresh Token
}

// 保存时指定类型
saveUserToken(user, accessToken, TokenType.ACCESS);
saveUserToken(user, refreshToken, TokenType.REFRESH);

// 验证时检查类型
boolean isRefreshTokenValid = tokenRepository.findByToken(refreshToken)
        .map(t -> t.getTokenType() == TokenType.REFRESH && !t.isExpired() && !t.isRevoked())
        .orElse(false);
```

**影响的文件**：
- ✅ `TokenType.java`
- ✅ `AuthenticationService.java`
- ✅ `JwtAuthenticationFilter.java`

---

## 📊 优化对比

| 优化项 | 优化前 | 优化后 |
|--------|--------|--------|
| **username/email** | ❌ 注册时未设置 username | ✅ username = email 一致性 |
| **Refresh Token** | ❌ 返回原 token | ✅ 令牌旋转，每次生成新 token |
| **错误响应** | ❌ 空响应或 200 | ✅ 明确状态码 + JSON 错误体 |
| **事务管理** | ❌ 无事务 | ✅ @Transactional 保证一致性 |
| **速率限制** | ❌ 无限制 | ✅ 配置建议 + 注释提醒 |
| **Content-Type** | ❌ 未设置 | ✅ application/json |
| **Token 持久化** | ❌ 仅 access token | ✅ access + refresh 都持久化 |

---

## 🔐 安全增强

### 令牌旋转的安全优势

1. **降低窃取风险**：即使 refresh token 被盗，也会在下次刷新时失效
2. **可审计性**：每次刷新都有记录，便于追踪异常行为
3. **自动失效**：旧 token 自动被标记为已撤销

### 黑名单机制

- ✅ Access Token 黑名单
- ✅ Refresh Token 黑名单
- ✅ 数据库持久化
- ✅ 支持手动撤销

---

## 🧪 测试场景

### 1. 注册测试
```bash
POST /api/v1/auth/register
{
  "firstname": "John",
  "lastname": "Doe",
  "email": "john@example.com",
  "password": "password123"
}

# ✅ 应返回 access_token 和 refresh_token
# ✅ username 应等于 email
```

### 2. 登录测试
```bash
POST /api/v1/auth/authenticate
{
  "username": "admin",
  "password": "admin"
}

# ✅ 应返回新的 access_token 和 refresh_token
# ✅ 旧 token 应被撤销
```

### 3. 刷新 Token 测试
```bash
POST /api/v1/auth/refresh-token
Authorization: Bearer [refresh_token]

# ✅ 应返回新的 access_token 和新的 refresh_token
# ✅ 旧的 refresh_token 应被撤销
```

### 4. 错误场景测试

**无效的 Refresh Token**：
```bash
POST /api/v1/auth/refresh-token
Authorization: Bearer invalid_token

# ✅ 应返回 401 + 错误 JSON
{
  "error": "Unauthorized",
  "message": "Invalid refresh token",
  "status": 401,
  "timestamp": 1697123456789
}
```

**缺失 Authorization Header**：
```bash
POST /api/v1/auth/refresh-token

# ✅ 应返回 400 + 错误 JSON
{
  "error": "Bad Request",
  "message": "Missing or invalid Authorization header",
  "status": 400,
  "timestamp": 1697123456789
}
```

---

## 📝 数据库变更

### Token 表结构
```sql
CREATE TABLE token (
  id INTEGER PRIMARY KEY,
  token VARCHAR(512) UNIQUE,
  token_type VARCHAR(20),  -- ✅ 新增：BEARER/ACCESS/REFRESH
  expired BOOLEAN,
  revoked BOOLEAN,
  user_id INTEGER REFERENCES _user(id)
);
```

### User 表结构
```sql
CREATE TABLE _user (
  id INTEGER PRIMARY KEY,
  username VARCHAR(255) UNIQUE,  -- ✅ 必须设置
  email VARCHAR(255),
  password VARCHAR(255),
  firstname VARCHAR(255),
  lastname VARCHAR(255),
  enabled BOOLEAN
);
```

---

## 🚀 部署建议

### 生产环境检查清单

- [ ] 配置速率限制（Bucket4j / Redis）
- [ ] 设置合理的 token 过期时间
  - Access Token: 15-30 分钟
  - Refresh Token: 7-30 天
- [ ] 启用 HTTPS
- [ ] 配置 CORS 白名单
- [ ] 监控异常刷新行为
- [ ] 定期清理过期 token 记录
- [ ] 实施 IP 白名单（可选）

### 监控指标

- Token 刷新频率
- 刷新失败次数
- 异常 IP 访问
- Token 撤销记录

---

## 📚 相关文档

- [JWT Best Practices](https://datatracker.ietf.org/doc/html/rfc8725)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [OAuth 2.0 Token Refresh](https://oauth.net/2/grant-types/refresh-token/)

---

## ✅ 总结

所有7项优化已全部实现：

1. ✅ Username/Email 一致性
2. ✅ Refresh Token 旋转
3. ✅ 明确的错误响应
4. ✅ 事务一致性
5. ✅ 访问控制建议
6. ✅ Content-Type 设置
7. ✅ 完整的 Token 黑名单

**下一步**：
1. 重新编译并启动应用
2. 测试所有认证流程
3. 验证 token 旋转机制
4. 检查错误响应格式

